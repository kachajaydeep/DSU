#include <stdio.h>
// It is a header file library that let's work with input and output functions, such as printf() it also adds the functionality to c program.

int main()
{
     int myNum = 5;
     // int a =10;
     // int *ptr;
     // ptr = &a;
     printf("%d\n", myNum);
}